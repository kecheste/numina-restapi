# Numina Arq worker app

app = "numina-worker"

[build]
  dockerfile = "Dockerfile"

[env]

[processes]
worker = "python -m app.worker.run_worker"

# Worker does not serve HTTP
# [[http_service]] section is intentionally omitted

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
